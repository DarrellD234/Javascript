// = assignment operator
// == comparison operator (Values are equal or not)
// === strict equality operator (COmpare if values and datatypes are equal)
// != inequality operator
// !== strict inequality operator (Not equal value or type)
// E.g 5 !== `5`  is true because although value is same, different type

const PI = 3.14;

if(PI !== "3.14"){
    myText1.textContent = `This is NOT PI`;
}
else{
    myText1.textContent = `This is PI`;
}